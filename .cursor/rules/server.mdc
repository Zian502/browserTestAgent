---
description: Nest server — LangGraph agents, skills, tools, Playwright, cache
globs: packages/server/**/*.ts
alwaysApply: false
---

# Server (`@browser-test-agent/server`)

- **框架**：NestJS；启动 `src/main.ts`，默认端口 `3850`（`PORT`）。
- **Agent 图**：`src/agents/graph.ts`；状态 `src/agents/state.ts`；各节点在 `agents/*-agent.ts`。
- **技能**：`src/skills/` + `registry.ts`；**工具**：`src/tools/`（`read` / `write` / `playwright`）。
- **Playwright**：`src/lib/playwright-browser-session.ts`；子操作见 `tools/playwright.ts` 与 `tools/index.ts` 的 `executeCoreTool`。
- **缓存**：`src/lib/file-cache.ts`，根目录通常为 `packages/server/.agent-cache/`（运行期写入）。
- **LLM**：`src/agents/llm-client.ts`（OpenAI 兼容；密钥与 base URL 环境变量见该文件）。
- **PageSpeed**：`src/mcps/pagespeed-mcp.ts`；无 API key 时返回 stub，勿当真实性能依据。
